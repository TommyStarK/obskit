---
# Source: tempo-distributed/templates/servicemonitor/ingester.yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: tempo-distributed-ingester
  namespace: obskit
  labels:
    app.kubernetes.io/name: tempo-distributed
    app.kubernetes.io/instance: tempo
    app.kubernetes.io/component: ingester
    app.kubernetes.io/part-of: memberlist
spec:
  endpoints:
    - port: http-metrics
      relabelings:
        - sourceLabels: [job]
          replacement: "obskit/ingester"
          targetLabel: job
      scheme: http
  namespaceSelector:
    matchNames:
    - obskit
  selector:
    matchLabels:
      app.kubernetes.io/name: tempo-distributed
      app.kubernetes.io/instance: tempo
      app.kubernetes.io/component: ingester
    matchExpressions:
      - key: prometheus.io/service-monitor
        operator: NotIn
        values:
          - "false"
